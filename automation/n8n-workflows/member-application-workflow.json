{
  "name": "ST-Solutions - Member Application Lifecycle Automation",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "member-application-lifecycle",
        "responseMode": "onReceived",
        "responseData": "allEntries",
        "options": {}
      },
      "name": "Webhook Ingest",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [100, 300]
    },
    {
      "parameters": {
        "functionCode": "// Canonical HMAC SHA-256 Signature Verification\nconst crypto = require('crypto');\n\nfunction canonicalStringify(obj) {\n  if (obj === null || typeof obj !== 'object') {\n    return JSON.stringify(obj);\n  }\n  if (Array.isArray(obj)) {\n    return '[' + obj.map(canonicalStringify).join(',') + ']';\n  }\n  const keys = Object.keys(obj).sort();\n  return '{' + keys.map(k => JSON.stringify(k) + ':' + canonicalStringify(obj[k])).join(',') + '}';\n}\n\nconst secret = $env.N8N_WEBHOOK_SECRET || 'your-32-character-secure-hmac-secret-here';\nconst headers = $node['Webhook Ingest'].json.headers;\nconst signature = headers['x-st-signature'];\nconst timestamp = headers['x-st-timestamp'];\nconst deliveryId = headers['x-st-delivery-id'] || $input.item.json.deliveryId;\n\nif (!signature || !timestamp || !deliveryId) {\n  throw new Error('Missing required security headers');\n}\n\nconst canonicalPayload = canonicalStringify($input.item.json);\nconst stringToSign = `${timestamp}.${deliveryId}.${canonicalPayload}`;\nconst calculated = crypto.createHmac('sha256', secret).update(stringToSign).digest('hex');\n\nif (signature !== calculated) {\n  throw new Error('HMAC signature verification failed: Invalid payload signature');\n}\n\nreturn $input.item;"
      },
      "name": "Verify Canonical Signature",
      "type": "n8n-nodes-base.function",
      "typeVersion": 1,
      "position": [320, 300]
    },
    {
      "parameters": {
        "conditions": {
          "string": [
            {
              "value1": "={{ $json.eventName }}",
              "value2": "member_application.approved"
            }
          ]
        }
      },
      "name": "Is Approved?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 1,
      "position": [560, 240]
    },
    {
      "parameters": {
        "fromEmail": "recruitment@st-solutions.com",
        "toEmail": "={{ $json.data.email }}",
        "subject": "Congratulations! Your ST-Solutions Application has been Approved",
        "html": "<h2>Welcome to ST-Solutions!</h2><p>Dear {{ $json.data.fullName }},</p><p>We are delighted to inform you that your application has been <strong>Approved</strong>!</p><p>Our operations team will issue your onboarding credentials and contract details shortly.</p><br/><p>Warm regards,<br/><strong>ST-Solutions Talent Acquisition</strong></p>"
      },
      "name": "Send Approval Email",
      "type": "n8n-nodes-base.emailSend",
      "typeVersion": 2,
      "position": [800, 160]
    },
    {
      "parameters": {
        "conditions": {
          "string": [
            {
              "value1": "={{ $json.eventName }}",
              "value2": "member_application.rejected"
            }
          ]
        }
      },
      "name": "Is Rejected?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 1,
      "position": [800, 320]
    },
    {
      "parameters": {
        "fromEmail": "recruitment@st-solutions.com",
        "toEmail": "={{ $json.data.email }}",
        "subject": "ST-Solutions Application Update",
        "html": "<p>Dear {{ $json.data.fullName }},</p><p>Thank you for taking the time to apply to ST-Solutions. After careful consideration, we will not be moving forward with your candidacy at this time.</p><p>We wish you every success in your future endeavors.</p><br/><p>Sincerely,<br/><strong>ST-Solutions Talent Team</strong></p>"
      },
      "name": "Send Rejection Email",
      "type": "n8n-nodes-base.emailSend",
      "typeVersion": 2,
      "position": [1040, 260]
    },
    {
      "parameters": {
        "fromEmail": "recruitment@st-solutions.com",
        "toEmail": "={{ $json.data.email }}",
        "subject": "ST-Solutions Application - Information Required / Status Update",
        "html": "<p>Dear {{ $json.data.fullName }},</p><p>Your application status has been updated: <strong>{{ $json.data.status || 'Action Required' }}</strong>.</p><p>{{ $json.data.reviewNotes || 'Please check your application portal for further updates.' }}</p><br/><p>Best regards,<br/><strong>ST-Solutions Operations</strong></p>"
      },
      "name": "Send Status Update Email",
      "type": "n8n-nodes-base.emailSend",
      "typeVersion": 2,
      "position": [1040, 420]
    },
    {
      "parameters": {
        "functionCode": "// Build authenticated callback payload to report DELIVERED status\nconst crypto = require('crypto');\n\nfunction canonicalStringify(obj) {\n  if (obj === null || typeof obj !== 'object') {\n    return JSON.stringify(obj);\n  }\n  if (Array.isArray(obj)) {\n    return '[' + obj.map(canonicalStringify).join(',') + ']';\n  }\n  const keys = Object.keys(obj).sort();\n  return '{' + keys.map(k => JSON.stringify(k) + ':' + canonicalStringify(obj[k])).join(',') + '}';\n}\n\nconst secret = $env.N8N_WEBHOOK_SECRET || 'your-32-character-secure-hmac-secret-here';\nconst deliveryId = $node['Verify Canonical Signature'].json.deliveryId;\nconst callbackUrl = $node['Verify Canonical Signature'].json.callbackUrl || 'http://localhost:3000/api/v1/automation/callback';\nconst timestamp = new Date().toISOString();\n\nconst callbackBody = {\n  deliveryId: deliveryId,\n  status: 'DELIVERED',\n  responseCode: 200,\n  providerMessageId: 'n8n-applicant-' + Date.now(),\n  deliveredAt: timestamp\n};\n\nconst canonical = canonicalStringify(callbackBody);\nconst signature = crypto.createHmac('sha256', secret).update(`${timestamp}.${deliveryId}.${canonical}`).digest('hex');\n\nreturn [{\n  json: {\n    callbackUrl,\n    callbackBody,\n    headers: {\n      'Content-Type': 'application/json',\n      'X-ST-Signature': signature,\n      'X-ST-Timestamp': timestamp,\n      'X-ST-Delivery-Id': deliveryId\n    }\n  }\n}];"
      },
      "name": "Prepare Signed Callback",
      "type": "n8n-nodes-base.function",
      "typeVersion": 1,
      "position": [1280, 280]
    },
    {
      "parameters": {
        "requestMethod": "POST",
        "url": "={{ $json.callbackUrl }}",
        "options": {},
        "headerParametersUi": {
          "parameter": [
            { "name": "Content-Type", "value": "application/json" },
            { "name": "X-ST-Signature", "value": "={{ $json.headers['X-ST-Signature'] }}" },
            { "name": "X-ST-Timestamp", "value": "={{ $json.headers['X-ST-Timestamp'] }}" },
            { "name": "X-ST-Delivery-Id", "value": "={{ $json.headers['X-ST-Delivery-Id'] }}" }
          ]
        },
        "jsonParameters": true,
        "body": "={{ JSON.stringify($json.callbackBody) }}"
      },
      "name": "Report Delivery Callback",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 3,
      "position": [1500, 280]
    }
  ],
  "connections": {
    "Webhook Ingest": {
      "main": [
        [
          {
            "node": "Verify Canonical Signature",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Verify Canonical Signature": {
      "main": [
        [
          {
            "node": "Is Approved?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Is Approved?": {
      "main": [
        [
          {
            "node": "Send Approval Email",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Is Rejected?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Is Rejected?": {
      "main": [
        [
          {
            "node": "Send Rejection Email",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Send Status Update Email",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Send Approval Email": {
      "main": [
        [
          {
            "node": "Prepare Signed Callback",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Send Rejection Email": {
      "main": [
        [
          {
            "node": "Prepare Signed Callback",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Send Status Update Email": {
      "main": [
        [
          {
            "node": "Prepare Signed Callback",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare Signed Callback": {
      "main": [
        [
          {
            "node": "Report Delivery Callback",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  }
}
